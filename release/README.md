# QA-AI Release Baseline

This directory contains revision-bound release evidence generated by Phase 20.

- `manifest.json` — generated capability and repository baseline.
- `validation-report.json` — fail-closed subsystem validation evidence.
- `RELEASE-NOTES.md` — human-readable release summary.

Generated evidence is not authoritative framework content. Re-run release validation after repository changes. Human approval is required before a candidate is declared Released.
