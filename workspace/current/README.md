# Current Workspace

`workspace/current/` is an optional convenience area for active local work.

It is not the authoritative long-term project/feature source of truth. Canonical operational baselines belong under:

```text
workspace/projects/<project-id>/features/<feature-id>/
```

Do not treat files in this directory as approved project artifacts unless they are registered and managed through the canonical feature workspace lifecycle.
